---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: centrals.media.flussonic.com
spec:
  group: media.flussonic.com
  names:
    kind: Central
    listKind: CentralList
    plural: centrals
    singular: central
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Central is the Schema for the centrals API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CentralSpec defines the desired state of Central
            properties:
              api_key:
                description: API_KEY is used to access Central API
                type: string
              api_url:
                description: API_URL used for setting the hostname and port under
                  which Central is accessible by Flussonic for CONFIG_EXTERNAL and
                  http_proxy requests
                type: string
              database:
                description: Postgresql Database connection string You must setup
                  it by yourself
                type: string
              dynamic_streams_auth_token:
                description: Token for accessing dynamic streams
                type: string
              edit_auth:
                description: Credentials for administrator access to the Central Admin
                  UI.
                type: string
              image:
                description: Central Docker Image (https://hub.docker.com/r/flussonic/central)
                type: string
              log_level:
                description: Logging level
                enum:
                - debug
                - info
                - error
                type: string
              log_requests:
                description: Enables logging HTTP-requests
                type: boolean
              nodeSelector:
                additionalProperties:
                  type: string
                description: Selector for nodes with running central worker instances
                type: object
              provisioner_cluster_key:
                description: API key for accessing mediaserver instances provisioned
                  to Central
                type: string
              provisioner_selector:
                description: Pod selector for locating mediaserver instances. If not
                  empty - Central will automatically provision streamers to cluster
                  from k8s API
                type: string
            required:
            - api_key
            - api_url
            - database
            - image
            type: object
          status:
            description: CentralStatus defines the observed state of Central
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
